syntax: program [-x|-y] [-n COUNT] -s STR
    -x and -y are optional and mutually exclusive
    -s STR - mandatory, STR is a string
    -n COUNT - optional, COUNT is non-negative integer (default: 10)
